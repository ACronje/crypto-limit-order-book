{
    "id": "/limitOrder",
    "type": "object",
    "properties": {
        "quantity": {
            "type": "integer",
            "minimum": 1
        },
        "price": {
            "type": "integer",
            "minimum": 1
        },
        "side": {
            "type": "string",
            "enum": ["buy", "sell"]
        },
        "postOnly": {
            "type": "boolean"
        }
    },
    "required": ["quantity", "price", "side"]
}